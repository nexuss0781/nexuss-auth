services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: nex_auth
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - nex_auth_postgres:/var/lib/postgresql/data

volumes:
  nex_auth_postgres:
