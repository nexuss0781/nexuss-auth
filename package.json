{
  "name": "nex-auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @nex-auth/sdk && npm run build -w @nex-auth/server",
    "test": "npm run test -w @nex-auth/sdk && npm run test -w @nex-auth/server",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
